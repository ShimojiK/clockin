p comments

- @comments.each do |comment|
  div= comment.body

= form_for @new_comment, url: { controller: "user/comments", action: :create } do |f|
  div
    = f.label :body, "コメント"
    = f.text_field :body, autocomplete: :off
  = f.submit "送信"
